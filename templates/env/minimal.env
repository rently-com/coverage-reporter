# GitHub Coverage Reporter - Minimal Configuration
# Only required variables when using .gcr.json

# GitHub Configuration (Required)
# ------------------------------
GITHUB_ACCESS_TOKEN=                # GitHub token with repo permissions
GITHUB_SHA=                  # Commit SHA for status checks (usually set by CI)
GITHUB_CURR_BRANCH=          # Current branch name (typically set by CI)

# AWS S3 Configuration (Optional)
# -----------------------------
# Only needed if storing coverage history in S3
AWS_S3_BUCKET=               # S3 bucket name
AWS_REGION=                  # AWS region for S3 bucket
AWS_ACCESS_KEY_ID=           # AWS access key ID
AWS_SECRET_ACCESS_KEY=       # AWS secret access key
