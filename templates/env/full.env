# GitHub Coverage Reporter Configuration
# ----------------------------------------

# GitHub Configuration (Required)
# ------------------------------
# GitHub access token with repo permissions
GITHUB_ACCESS_TOKEN={{GITHUB_ACCESS_TOKEN}}
GITHUB_OWNER=                # GitHub organization or username
GITHUB_REPO=                 # Repository name (without owner)
GITHUB_SHA=                  # Commit SHA for status checks
GITHUB_CURR_BRANCH=          # Current branch name (typically set by CI)
GITHUB_PR_NUMBER=            # Pull request number (if applicable)
# Target/Base branch for comparison (usually 'main' or 'master')
GITHUB_TARGET_BRANCH={{GITHUB_TARGET_BRANCH}}

# Coverage Thresholds
# ------------------
# Set the minimum acceptable coverage percentages (0-100)
{{COVERAGE_THRESHOLDS}}

# Coverage File Paths (REQUIRED)
# ------------------
# Paths to coverage-summary.json files for each type
# These must be set for each coverage type you want to report
{{COVERAGE_FILE_PATHS}}

# AWS S3 Configuration (Optional)
# -----------------------------
# Only needed if storing coverage history in S3
AWS_S3_BUCKET=               # S3 bucket name
FOLDER_NAME=                 # S3 folder name for organization
AWS_REGION=                  # AWS region for S3 bucket
FILE_NAME=coverage-history.json         # S3 file name (set in .gcr.json under config.s3.fileName)
AWS_ACCESS_KEY_ID=           # AWS access key ID
AWS_SECRET_ACCESS_KEY=       # AWS secret access key
AWS_SESSION_TOKEN=           # AWS session token (temporary credentials)
